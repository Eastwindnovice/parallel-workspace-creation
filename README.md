# parallel-workspace-creation
Allow Claude Code to manage worktree branches while also placing the worktree branches in the same parent directory as the main repository for easier development. This can be achieved by using the `WorktreeCreate` hook function.
